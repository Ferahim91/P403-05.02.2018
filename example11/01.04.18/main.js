




submitButton.onclick=function(){
    console.log(select.value);
    var baseValue=baseInput.value;
    var powerValue=baseInput.power;
    if(baseValue && powerValue);
    {
        resaultInput.value= (baseValue,powerValue);
    }
}
